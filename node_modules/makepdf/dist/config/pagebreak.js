"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.pageBreakBefore = void 0;
function pageBreakBefore(cur, follow, nextPage) {
    var lvl = cur.headlineLevel;
    if (cur.table) {
        follow = follow.filter(function (n) { return n.style !== "tableHeader"; });
    }
    else if (!lvl) {
        return;
    }
    var footIdx = follow.findIndex(function (n) { return n.style === "footer"; });
    if (footIdx >= 0)
        follow = follow.slice(0, footIdx);
    if (!follow.length &&
        nextPage.length &&
        !(nextPage[0].headlineLevel <= lvl) &&
        !(nextPage[0].style === "footer")) {
        return true;
    }
}
exports.pageBreakBefore = pageBreakBefore;
